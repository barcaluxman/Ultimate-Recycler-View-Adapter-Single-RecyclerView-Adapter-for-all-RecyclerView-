<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.gurkhatech.mvppatterntest">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name=".githubList.utils.GithubApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login.view.MainActivity">
        </activity>
        <activity android:name=".orientationinfo.view.HomeActivity"/>
        <activity android:name=".colorlist.view.ColorsListActivity">
        </activity>
        <activity android:name=".rximplementation.view.RxActivity">
        </activity>
        <activity android:name=".githubList.view.GithubListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>